package com.example.tapitapi.`interface`

import okhttp3.OkHttpClient
import retrofit2.Retrofit

class ServiseGenerator {
//    val baseurl = "https://tapioka-searcher.yanai-k.com/"
//    private val httpClient:OkHttpClient.Builder = OkHttpClient.Builder()
//    private val builder:Retrofit.Builder = Retrofit.Builder().baseUrl(baseurl)
//    fun createService(serviceClass: Class<Any>):Class<S>{
//        val retrofit =builder.client(httpClient.build()).build()
//        return retrofit.create(serviceClass!!)
//    }
}